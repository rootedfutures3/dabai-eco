-- ============================================================
-- 貼文紀錄：記下是誰按的發布
-- ============================================================
-- 為什麼需要：一鍵發布會真的把東西送到公開的粉專上。
-- 事後看到一篇不該發的，要查得出是誰按的 ——
-- 這是責任歸屬，不是監控。團隊裡有幾個人有發文權限，
-- 沒有這一欄的話，出事只能靠問。
--
-- 存的是登入用的 Gmail：它是唯一的，而且不會因為
-- 之後改了顯示名稱就對不上。名字在畫面上即時查，
-- 查不到（帳號被刪了）就顯示信箱本身。
--
-- 怎麼跑：Supabase → SQL Editor → New query → 貼上 → Run
-- 可以重複執行。
-- ============================================================

alter table posts add column if not exists by_user text;

-- 核對
select id, at, channel, status, by_user from posts order by id desc limit 5;
